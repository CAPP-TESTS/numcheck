{
  "buildCommand": "vite build && npx esbuild lib/extractor.ts lib/phone-checks.ts lib/url-checks.ts --outdir=lib --format=esm --platform=node --packages=external",
  "outputDirectory": "dist",
  "functions": {
    "api/analyze.ts": {
      "maxDuration": 60,
      "includeFiles": "lib/**"
    }
  },
  "rewrites": [
    { "source": "/api/(.*)", "destination": "/api/$1" },
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
